<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    tools:context=".breathing">

    <ImageButton
        android:id="@+id/breathingbglungs"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:src="@drawable/breathingbg"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:scaleType="centerInside"
        android:animateLayoutChanges="true"
        android:backgroundTint="@color/transparent"/>

    <ImageButton
        android:id="@+id/breathingImage"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/breathingimage"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:scaleType="centerInside"
        android:animateLayoutChanges="true"
        android:backgroundTint="@color/transparent"/>

    <TextView
        android:id="@+id/timerTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Timer: 0"
        android:textSize="28sp"
        android:paddingBottom="30dp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@+id/breathingImage"
        app:layout_constraintEnd_toEndOf="@id/breathingImage"
        app:layout_constraintHorizontal_bias="0.504"
        app:layout_constraintStart_toStartOf="@id/breathingImage"
        app:layout_constraintTop_toTopOf="@id/breathingImage"
        app:layout_constraintVertical_bias="1.0" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/startButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start"
        android:background="@drawable/mybutton"
        app:layout_constraintTop_toBottomOf="@id/breathingImage"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
